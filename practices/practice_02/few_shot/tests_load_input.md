# Нагрузочные проверки

Цель — подтвердить, что ограничения из [`adr.md`](adr.md) (таймаут 10 с по REL-1, отказ 413 до вызова LLM по API-1) держатся под параллельной нагрузкой, и что метрики из `problem.md` («доля 5xx», «среднее время ответа») действительно считаются по логам OBS-1. Внешний LLM заменяется mock-реализацией протокола `LLM` (`review_service.py:4-6`) с настраиваемой задержкой — реальный провайдер под нагрузку не ставим: его реализация, лимиты и стоимость токенов неизвестны (`context.md`; `problem.md` — риск неконтролируемых расходов).

Целевые уровни нагрузки (RPS, число одновременных пользователей) в источниках не заданы — значения ниже выбраны как минимальные для проверки поведения, а не как SLA, и требуют уточнения у владельца сервиса.

Все профили — `POST /api/reviews` через k6 в закрытой модели (фиксированное число VU, каждый VU шлёт следующий запрос после ответа), поэтому RPS ниже — производная от числа VU и задержки mock LLM, а не отдельная настройка нагрузки.

| Профиль | RPS | p95 latency | Error rate | Инструмент |
|---|---|---|---|---|
| Валидный diff ~5 000 симв., mock `generate` 0.5 с; 10 VU, 2 мин (базовая линия) | 10–20 | < 10 с (REL-1) | 5xx < 1% (цель `problem.md`); 100% ответов валидны по OUT-1 | k6 |
| Медленный LLM: mock `generate` спит 15 с; 10 VU, 1 мин (REL-1 под нагрузкой) | ~1 | ≤ ~10 с, max тоже ≤ ~10 с (+ накладные — требует уточнения) | 5xx = 0; 100% тел — контролируемый ответ OUT-1; открытых соединений не больше числа VU | k6 |
| Большой diff 20 001 симв.; 20 VU, 1 мин (отказ 413 по API-1) | ≥ 20 (нет вызова LLM, ограничен только валидацией) | существенно ниже профиля 1 (ориентир < 500 мс — требует уточнения) | 100% ответов 413; вызовов `mock_llm.generate` = 0 | k6 |
| Граница лимита: чередование 20 000 / 20 001 симв.; 10 VU, 1 мин | 10–20 | < 10 с (REL-1) | 50% 200 / 50% 413 (граница API-1 включительная); 5xx = 0 | k6 |
| Смешанный поток: 70% валидных, 15% `{}`, 15% diff > 20 000 симв.; 10 VU, 2 мин | 10–20 | < 10 с (REL-1) | 5xx = 0; доли 200/422/413 соответствуют профилю; на каждый запрос ровно одна запись лога OBS-1 без содержимого diff | k6 |
| SEC-1: diff 19 999 симв. с 50 вставками `token=...` и блоком `PRIVATE KEY`; 10 VU, 1 мин | 10–20 | < 10 с общего времени, включая этап редактирования (REL-1) | 5xx < 1%; секретов в перехваченных промптах (`mock_llm.last_prompt`) — 0% | k6 |

Evidence по профилям (почему каждый нужен):

1. `review_service.py:15` вызывает LLM синхронно без ограничений — профиль 1 фиксирует базовую линию по логу OBS-1 (`request_id`, длительность, статус).
2. `review_service.py:15`, `review_service.py:5`: синхронный `generate` без таймаута — без исправления каждый запрос будет висеть 15 с (P1-02, Risk 3).
3. `api.py:10` передаёт любой diff в сервис — без исправления вся нагрузка уйдёт в LLM (P1-02, Risk 2; `problem.md` — расход токенов). Измеряем счётчик вызовов mock LLM.
4. API-1: «длиннее 20 000» — граница включительная.
5. `api.py:10` — `KeyError` на `{}` даёт 500 (P1-02, check 5); OBS-1 — полнота лога (число записей = число запросов), отсутствие маркера `MARKER_XYZ` из валидных diff в логе.
6. `review_service.py:14` — этап редактирования добавляется решением `adr.md`, его стоимость на больших входах неизвестна; время этапа измеряем отдельно от вызова LLM.

Если нагрузочное тестирование пока не требуется, обоснуйте это и назовите условие, после которого оно понадобится.

Нагрузочное тестирование в объёме выше требуется уже сейчас в минимальном виде (сценарии 2 и 3), потому что проблема из `problem.md` — не только отдельные ошибки, а поведение открытого эндпоинта без лимитов под потоком запросов: неконтролируемые расходы на LLM-токены и зависание на долгих вызовах. Полноценное нагрузочное тестирование с реальным провайдером и целевыми SLA откладывается до момента, когда будут известны реализация LLM в `app.dependencies` и целевой RPS — оба факта в источниках отсутствуют и требуют уточнения.

## Как использовали AI

- Для чего: подбор нагрузочных сценариев, проверяющих таймаут REL-1, отказ 413 по API-1 и полноту логов OBS-1 под параллельными запросами через mock LLM, с явным указанием, какие пределы взяты из `problem.md`, а какие требуют уточнения.
- Тип промпта: master prompt (build).
- Строка в [`prompts.md`](prompts.md): P1-03.
- Что проверили и исправили сами: убедилась, что нагрузочные пределы явно помечены как "требует уточнения" там, где в источниках нет данных (целевой RPS, SLA), а не выданы как окончательные значения.
