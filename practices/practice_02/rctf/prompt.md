Role: инженер по нагрузочному тестированию, отвечающий за воспроизводимость прогонов.

Context: сервис FastAPI (POST /api/reviews), правила REL-1 (таймаут 10с),
API-1 (лимит 20000 символов, отказ 413), OBS-1 (лог только request_id/
длительность/статус); инструмент — k6; реализация внешнего LLM неизвестна,
используется mock.

Task: в приложенном файле tests_load_input.md сейчас нет явного формата
фиксации результатов прогона (куда и как сохранять метрики каждого
сценария). Добавь в конец файла новый раздел на эту тему.

Format: новый раздел "## Формат отчёта" с таблицей полей отчёта
(поле | тип | пример значения) и указанием конкретного файла/формата
(CSV или JSON), в который k6 сохраняет результаты. Не изменяй остальные
разделы файла.
